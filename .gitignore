# 作業用の参照資料（第三者著作物を含むため配布しない）
temp/

# 公開リポジトリに含められないパッケージの置き場（ADR-0002）。
# カード実装と、それを名指しして渡すところが入る
private/

# 依存
node_modules/

# ビルド出力
dist/

# 手元で立てたときの置き場（ADR-0018）。打った対戦がそのまま入る
*.sqlite
*.sqlite-shm
*.sqlite-wal

# ローカル設定
.claude/settings.local.json
CLAUDE.local.md

# エディタ（JetBrains IDE）
.idea/
*.iml
