# 総合ルールの条番号をテストに対応づける

ルールの挙動を検証するテストには、根拠となる総合ルールの条番号を付ける。総合ルールに含まれる 87 個の【例】は、PRLLP 自身が書いた「この状況ではこうなる」という記述であり、そのまま受け入れテストとして写せる。

## 影響

- 進捗を「総合ルールの何条を実装・検証したか」として測れる。機能単位の主観的な進捗率より正確になる。
- 裁定でもめたとき、条番号からテストへ、テストからコードへ辿れる。
- テスト名に付いた番号は装飾ではない。整理や改名の対象にしないこと。
