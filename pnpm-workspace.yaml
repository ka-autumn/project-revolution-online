packages:
  - packages/*
  # 公開リポジトリに含められないパッケージ（ADR-0002）。ここに無くても
  # pnpm verify は通る——対戦を動かすときだけ要る
  - private/*
allowBuilds:
  esbuild: true

# テストを持つパッケージが自分で vitest を宣言できるようにする。
# バージョンはここ 1 箇所で決める。
catalog:
  vitest: ^4.1.10
